# Regenerable outputs, kept out of git the way the head-based study keeps them out.
# cache/ is the pickled Performance pipeline and the residuals npz -- ~1.5 GB per arm set, and
# rebuilt by re-running the plot scripts. figures/ is redrawn from that cache in seconds.
cache/
figures/
